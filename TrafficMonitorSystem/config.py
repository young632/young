# -*- coding: utf-8 -*-
"""
智能交通视觉监测系统 - 配置模块
包含所有固定参数和阈值设定
"""

import cv2
import numpy as np

# =============================================================================
# 视频/图像参数
# =============================================================================
VIDEO_WIDTH = 1280
VIDEO_HEIGHT = 720
FPS = 30

# =============================================================================
# 车流量统计参数 - 虚拟线圈
# =============================================================================
DETECTION_LINE_Y_RATIO = 0.7
MIN_CONTOUR_AREA = 500
MAX_CONTOUR_AREA = 50000
VEHICLE_HISTORY_SIZE = 10

# =============================================================================
# 背景减法参数 (MOG2)
# =============================================================================
MOG2_HISTORY = 100
MOG2_VAR_THRESHOLD = 50
MOG2_SHADOW_DETECTION = False

# =============================================================================
# 车速检测参数
# =============================================================================
SPEED_CALC_FRAMES = 5
MIN_TRACK_FRAMES = 3
PIXELS_TO_METERS = 0.05
SPEED_THRESHOLD = 80.0
SPEED_SMOOTHING = 0.3

# =============================================================================
# 热力图参数
# =============================================================================
HEATMAP_DECAY = 0.95
HEATMAP_THRESHOLD = 0.3
HEATMAP_RADIUS = 15
HEATMAP_COLORS = [
    (255, 0, 0),       # 蓝色-低
    (0, 255, 0),       # 绿色-中
    (0, 255, 255),     # 黄色-高
    (0, 0, 255),       # 红色-拥堵
]

# =============================================================================
# 车道线检测参数
# =============================================================================
# Canny边缘检测
CANNY_LOW = 50
CANNY_HIGH = 150

# HSL颜色阈值
HSL_WHITE_LOW = np.array([0, 200, 0], dtype=np.uint8)
HSL_WHITE_HIGH = np.array([255, 255, 255], dtype=np.uint8)
HSL_YELLOW_LOW = np.array([15, 40, 100], dtype=np.uint8)
HSL_YELLOW_HIGH = np.array([35, 255, 255], dtype=np.uint8)

# 高斯滤波
GAUSSIAN_KERNEL = (5, 5)

# ROI区域 (多边形顶点比例)
ROI_VERTICES = np.array([
    [0.05, 1.0],   # 左下
    [0.40, 0.55],  # 左上
    [0.60, 0.55],  # 右上
    [0.95, 1.0]    # 右下
], dtype=np.float32)

# 霍夫变换
HOUGH_RHO = 1
HOUGH_THETA = np.pi / 180
HOUGH_THRESHOLD = 15
HOUGH_MIN_LINE_LENGTH = 20
HOUGH_MAX_LINE_GAP = 30

# 车道线斜率过滤
MIN_SLOPE = 0.3

# =============================================================================
# 轨迹跟踪参数
# =============================================================================
MAX_DISAPPEARED = 10
MAX_DISTANCE = 50

# =============================================================================
# 显示参数
# =============================================================================
DISPLAY_FONT = cv2.FONT_HERSHEY_SIMPLEX
DISPLAY_FONT_SCALE = 0.7
DISPLAY_THICKNESS = 2
INFO_PANEL_ALPHA = 0.7

# =============================================================================
# 颜色定义
# =============================================================================
COLOR_RED = (0, 0, 255)
COLOR_GREEN = (0, 255, 0)
COLOR_BLUE = (255, 0, 0)
COLOR_YELLOW = (0, 255, 255)
COLOR_WHITE = (255, 255, 255)
COLOR_ORANGE = (0, 165, 255)
COLOR_CYAN = (255, 255, 0)

# =============================================================================
# UI布局
# =============================================================================
PANEL_WIDTH = 200
PANEL_MARGIN = 10
INFO_BOX_PADDING = 5